<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.project.UsERP.persistence.AdDAO">

	<!-- 회계보고서 - 재무상태표 -->
	
	<!-- 회계보고서 - 손익계산서 -->
	
	<!-- 계좌 관리 - 계좌 리스트 -->
	<select id = "bankList" resultType = "com.project.UsERP.vo.BankVO">
		SELECT * FROM bank ORDER BY bank_code
	</select>
	
	<!-- 계정 관리 - 계정 리스트 -->
	<select id = "accontList" resultType = "com.project.UsERP.vo.AccountVO">
		SELECT * FROM accont ORDER BY acco_code
	</select>
	
	<!-- 전표 관리 - 회계 전표 -->
	<select id = "statementList" resultType = "com.project.UsERP.vo.AccountStatementVO">
		SELECT * FROM account_statement ORDER BY accs_code
	</select>
	<!-- 전표 관리 - 급여 전표 -->
	<select id = "salarystatementList" resultType = "com.project.UsERP.vo.SalaryStatementVO">
		SELECT * FROM salary_statement ORDER BY ss_code
	</select>
	<!-- 전표 관리 - 채권/채무 조회 -->
	

</mapper>